// Define afunction to print each magician name from an array of 

function show_magicians(magicians: string[]){
  magicians.forEach(name => console.log(name));
}

// define an array containg magicains name
let magicains_names = ["Harry poter", "Manahil", "Waqas"]


// callling the function to print each magician name 
show_magicians(magicains_names)